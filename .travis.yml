# To create a release
#   git checkout master
#   git pull
# modify version number in package.json in master branch
#   git checkout -b deploy
#   git pull
#   git rebase origin/master
#   git commit -a -m "Release"
#   git push
#   git checkout master

language: generic
os: linux
dist: xenial

jobs:
  include:
    - os: osx
      language: node_js
      node_js: "6.16"
      branches:
        only: master

before_install:
  - npm install && npm ls
  - head -30 node_modules/hapi-server-verifier/is.js

script:
  - npm test

notifications:
  email:
    recipients:
      - ksreddy9945@gmail.com
      - rweigel@gmu.edu
  on_success: change
  on_failure: always
